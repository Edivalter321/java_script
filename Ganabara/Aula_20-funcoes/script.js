function parimp(n) {
  // n = parâmetro

  if (n % 2 == 0) {
    // ação
    return "par";
  } else {
    return "ímpar";
  }
}
let res = parimp(11); // parimp = chamada
// console.log(parim(11))
console.log(res); // usar assim

